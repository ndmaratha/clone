package main

import "fmt"

func swap(x, y int) (int, int) {
    return y, x
}

func main() {
    a, b := 10, 20

    
    a, b = swap(a, b)

    
    fmt.Println("After swapping:")
    fmt.Println("a =", a)
    fmt.Println("b =", b)
}